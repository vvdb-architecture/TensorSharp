--- a/src/ggml-cuda/ggml-cuda.cu
+++ b/src/ggml-cuda/ggml-cuda.cu
@@ -1500,6 +1500,12 @@
     size_t nbd3 = dst->nb[3];
 
     cublasComputeType_t cu_compute_type = traits::compute_type;
+#if !defined(GGML_USE_HIP) && !defined(GGML_USE_MUSA)
+    // An explicit F32 input request must not inherit the handle's TF32 math mode.
+    if (compute_type == GGML_TYPE_F32 && dst->op_params[3] == GGML_PREC_F32) {
+        cu_compute_type = CUBLAS_COMPUTE_32F_PEDANTIC;
+    }
+#endif
     cudaDataType_t cu_data_type = traits::data_type;
     cudaDataType_t cu_data_type_a = traits::data_type;
     cudaDataType_t cu_data_type_b = traits::data_type;
@@ -1540,7 +1546,9 @@
     // Theoretically cublasGemmStridedBatchedEx would always work, even for a single matrix.
     // However, for some old NVIDIA and AMD GPUs the strided/Ex GEMM is much slower,
     //     probably because the internal kernel selection logic is suboptimal.
-    if (compute_type == GGML_TYPE_F32 && ne12 == 1 && ne13 == 1) {
+    // Sgemm inherits handle math mode; use Ex below for explicit source precision.
+    if (compute_type == GGML_TYPE_F32 && ne12 == 1 && ne13 == 1 &&
+        dst->op_params[3] != GGML_PREC_F32) {
         CUBLAS_CHECK(
             cublasSgemm(cublas_h, CUBLAS_OP_T, CUBLAS_OP_N,
                     ne01, ne11, ne10,
@@ -1626,12 +1634,12 @@
     } else if (compute_type == GGML_TYPE_F16 && !fast_fp16_hardware_available(ggml_cuda_info().devices[ctx.device].cc)) {
         compute_type = GGML_TYPE_F32;
     }
-    if (dst->op_params[0] == GGML_PREC_F32) {
+    if (dst->op_params[0] == GGML_PREC_F32 || dst->op_params[3] == GGML_PREC_F32) {
         compute_type = GGML_TYPE_F32;
     }
 
     const char * env_c = getenv("GGML_CUDA_CUBLAS_COMPUTE_TYPE");
-    if (env_c != nullptr) {
+    if (env_c != nullptr && dst->op_params[3] != GGML_PREC_F32) {
         std::string env_cpp = env_c;
         for (char & c : env_cpp) {
             c = std::tolower(c);
@@ -1762,6 +1770,7 @@
     const ggml_tensor * dst  = tensor;
 
     const bool is_mul_mat_id = tensor->op == GGML_OP_MUL_MAT_ID;
+    if (dst->op_params[3] == GGML_PREC_F32 && src0->type != GGML_TYPE_F32) return false;
 
     bool use_mul_mat_vec_f =
         (src0->type == GGML_TYPE_F32 || src0->type == GGML_TYPE_F16 || src0->type == GGML_TYPE_BF16) &&
@@ -1784,6 +1793,7 @@
 }
 
 static bool ggml_cuda_should_fuse_mul_mat_vec_q(const ggml_tensor * tensor) {
+    if (tensor->op_params[3] == GGML_PREC_F32) return false;
     ggml_tensor *       src0 = tensor->src[0];
     ggml_tensor *       src1 = tensor->src[1];
     const ggml_tensor * dst  = tensor;
@@ -1832,6 +1842,11 @@
 
     const int cc        = ggml_cuda_info().devices[ctx.device].cc;
     const int warp_size = ggml_cuda_info().devices[ctx.device].warp_size;
+
+    if (dst->op_params[3] == GGML_PREC_F32 && src0->type != GGML_TYPE_F32) {
+        ggml_cuda_mul_mat_cublas(ctx, src0, src1, dst);
+        return;
+    }
 
     if (ggml_cuda_should_use_mmvf(src0->type, cc, src0->ne, src0->nb, ne11)) {
         // The custom F16 vector kernel can be used over batched cuBLAS GEMM.
@@ -1853,7 +1868,9 @@
         ggml_cuda_mul_mat_vec_f(ctx, src1, src0, nullptr, &dst_vec);
         return;
     }
-    if (ggml_cuda_should_use_mmf(src0->type, cc, warp_size, src0->ne, src0->nb, ne11, /*mul_mat_id =*/ false)) {
+    // MMF uses TF32 for F32 inputs, so it cannot preserve an explicit F32 source.
+    if (dst->op_params[3] != GGML_PREC_F32 &&
+        ggml_cuda_should_use_mmf(src0->type, cc, warp_size, src0->ne, src0->nb, ne11, /*mul_mat_id =*/ false)) {
         ggml_cuda_mul_mat_f(ctx, src0, src1, nullptr, dst);
         return;
     }
@@ -1877,13 +1894,17 @@
     if (src1->type != GGML_TYPE_F32 || dst->type != GGML_TYPE_F32) {
         return true;
     }
+    if (dst->op_params[3] == GGML_PREC_F32 && src0->type != GGML_TYPE_F32) {
+        return true;
+    }
 
     if (dst->ne[2] <= MMVQ_MAX_BATCH_SIZE) {
         if (ggml_is_quantized(src0->type)) {
             if (dst->ne[2] <= get_mmvq_mmid_max_batch(src0->type, cc)) {
                 return false;
             }
-        } else if (GGML_CUDA_CC_IS_AMD(cc)) {
+        } else if (GGML_CUDA_CC_IS_AMD(cc) ||
+                   (src0->type == GGML_TYPE_F32 && dst->op_params[3] == GGML_PREC_F32)) {
             return false;
         }
     }
@@ -1892,7 +1913,8 @@
         return false;
     }
 
-    if (ggml_cuda_should_use_mmf(src0->type, cc, WARP_SIZE, src0->ne, src0->nb, src1->ne[2], /*mul_mat_id=*/true)) {
+    if (dst->op_params[3] != GGML_PREC_F32 &&
+        ggml_cuda_should_use_mmf(src0->type, cc, WARP_SIZE, src0->ne, src0->nb, src1->ne[2], /*mul_mat_id=*/true)) {
         return false;
     }
 
@@ -1912,7 +1934,8 @@
     const int cc = ggml_cuda_info().devices[ggml_cuda_get_device()].cc;
 
     // [TAG_MUL_MAT_ID_CUDA_GRAPHS]
-    if (src1->type == GGML_TYPE_F32 && dst->type == GGML_TYPE_F32) {
+    if (src1->type == GGML_TYPE_F32 && dst->type == GGML_TYPE_F32 &&
+        (dst->op_params[3] != GGML_PREC_F32 || src0->type == GGML_TYPE_F32)) {
         static_assert(MMVQ_MAX_BATCH_SIZE == MMVF_MAX_BATCH_SIZE);
         if (ne2 <= MMVQ_MAX_BATCH_SIZE) {
             if (ggml_is_quantized(src0->type)) {
@@ -1922,7 +1945,8 @@
                     return;
                 }
             } else {
-                if (GGML_CUDA_CC_IS_AMD(cc)) {
+                if (GGML_CUDA_CC_IS_AMD(cc) ||
+                    (src0->type == GGML_TYPE_F32 && dst->op_params[3] == GGML_PREC_F32)) {
                     ggml_cuda_mul_mat_vec_f(ctx, src0, src1, ids, dst);
                     return;
                 }
@@ -1934,7 +1958,8 @@
             return;
         }
 
-        if (ggml_cuda_should_use_mmf(src0->type, cc, WARP_SIZE, src0->ne, src0->nb, src1->ne[2], /*mul_mat_id=*/true)) {
+        if (dst->op_params[3] != GGML_PREC_F32 &&
+            ggml_cuda_should_use_mmf(src0->type, cc, WARP_SIZE, src0->ne, src0->nb, src1->ne[2], /*mul_mat_id=*/true)) {
             ggml_cuda_mul_mat_f(ctx, src0, src1, ids, dst);
             return;
         }
@@ -1947,7 +1972,8 @@
     GGML_ASSERT(nb12 % nb11 == 0);
     GGML_ASSERT(nb2  % nb1  == 0);
 
-    const ggml_type type_src1_sorted = (src0->type == GGML_TYPE_F16 && !fast_fp16_hardware_available(cc))
+    const ggml_type type_src1_sorted = dst->op_params[3] == GGML_PREC_F32 ||
+        (src0->type == GGML_TYPE_F16 && !fast_fp16_hardware_available(cc))
         || ggml_is_quantized(src0->type) ? GGML_TYPE_F32 : src0->type;
     const ggml_type type_dst_sorted  = GGML_TYPE_F32;
     const size_t ts_src1_sorted = ggml_type_size(type_src1_sorted);
@@ -2042,6 +2068,8 @@
         dst_slice.nb[2]  = dst_slice.ne[1] * dst_slice.nb[1];
         dst_slice.nb[3]  = dst_slice.ne[2] * dst_slice.nb[2];
         dst_slice.data   = dst_data_cur;
+        dst_slice.op_params[0] = dst->op_params[0];
+        dst_slice.op_params[3] = dst->op_params[3];
 
         ggml_cuda_mul_mat(ctx, &src0_slice, &src1_slice, &dst_slice);
         CUDA_CHECK(cudaGetLastError());
