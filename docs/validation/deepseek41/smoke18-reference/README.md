Final native `6b3` produced the expected `[22,1]` tokens (`4`, then EOS), and the fresh-slot traced replay returned bit-for-bit identical logits. The **strict CPU-oracle comparison still fails**: maximum absolute error **2.7089203596**, relative L2 **0.1462157145**, cosine **0.9898177485**. The top five IDs match. [Complete numerical results](final-run/comparison.json) preserve the unchanged `2e-5` tolerance; command exit 0 does not mean numerical parity.

The retained CPU reference explicitly used model-cache rounding and one unsplit 18-token prefill at position 0. [The read-only provenance report](provenance.json) preserves the exact log, SHA-256, prompt tokens and NumPy headers. The log records `cache_type=model` and one layer 0–39 pass; all 660 trace files have prefix `p000000`, the embedding shape is `(18,4,5120)`, and the logits shape is `(18,129280)`. Its original launcher argv, CPU thread count, matrix row-block size and creation-source hash were not retained and are not inferred from defaults.

The [final runner](../../../../eng/tests/dsv41-final-smoke18.py) verifies the reviewed native and reference log hashes, refuses to overwrite an output directory or load while any GPU compute process is resident, and reuses the retained reference without rerunning it. All eight [local guard checks](runner-guards.json) pass: matching reference evidence, mismatched top10/norm rejection, zero-norm rejection, inherited override cleanup, and the resident-GPU/output-overwrite guards. They use small arrays and a fake GPU-process listing; no native load or GPU execution occurred in these checks. The runner binds reference logits to the SHA-pinned log top10 and its F32-rounded norm, clears inherited `TS_DSV4_*`, `TS_DSV41_*` and `GGML_*` overrides before applying reviewed settings, and attaches the previously verified seven-shard manifest while checking current shard sizes. It does not rehash the 264 GB checkpoint.

The optional `--trace-after` extension preserves untraced primary generation, then allocates a fresh native slot and replays only the original prompt with tracing enabled. The primary slot is restored before freeing the diagnostic slot; model destruction remains the final cleanup for every allocated slot. Existing default behavior and `--trace` primary tracing remain available. The helper rejects inherited active tracing and conflicting trace modes when the primary must be untraced. All 25 [fake-ABI and trace-coverage checks](trace-extension-checks.json) pass, covering default behavior, exact replay, changed diagnostic logits, allocation/selection/forward/cleanup failures, environment restoration, and partial trace rejection. These tests execute no native code or GPU work.

Tracing can change graph partitioning and CUDA fusion, so diagnostic logits are compared separately against both the primary result and CPU oracle. Trace coverage requires exact-sized attention and FFN outputs for all 40 layers plus terminal logits, whose bytes must match the diagnostic forward's returned logits. It does not claim every possible intermediate is recorded. A diagnostic failure fails the command while retaining the primary metrics and original generated IDs. The [previously reviewed runner and helper](previous-reviewed/) are preserved byte-for-byte, with the original guard report and manifest.

The single guarded run started at **2026-09-11 13:24:14.918 UTC** after the final HTTP host stopped and the resident-GPU guard passed. It completed with exit 0; native process elapsed time was 248.97 seconds, including 239.59 seconds loading and a 6.02-second traced replay. GPU compute processes were verified empty at 13:29:49 UTC. These are diagnostic timings, not an end-to-end performance benchmark. [Completion and cleanup evidence](completion.json) records the command, timing scope and trace locations. Exact invocation:

```sh
/workspace/deepseek41-work/reference-venv/bin/python \
  /workspace/deepseek41-work/final-smoke18-runner.py --trace-after
```

The reviewed native settings are CUDA8 layer placement, context4096, ubatch128, threads24, CPU-MoE0, TP0, production flash/fused attention, and one18-token prefill followed by at most one decode. These match the retained old native smoke's load arguments. Engram full-table warming is disabled. The sparse/compact thresholds are not reached. This costs one complete checkpoint reload—originally235seconds, potentially longer under filesystem page faults—and must not coexist with another full-model allocation.

Artifacts are written to a new `/workspace/deepseek41-work/final-smoke18-6b3/` directory: source snapshots, exact command/settings/input hashes, reference log/last logits, native stdout/stderr, first logits, comparison and an artifact manifest. Compare only the last row of the retained reference to the native first-token logits. The report preserves strict2e-5 allclose, max absolute error, relative L2, cosine, top-five IDs and expected generated IDs `[22,1]`; it also recomputes the old native comparison for context. Matching arithmetic or greedy IDs does not establish strict numerical parity. The F32-input GGUF oracle does not reproduce native quantized activation kernels or establish original-FP8/llama.cpp equivalence.

The final logit payload is also bit-for-bit identical to the retained earlier `smoke-first-logits.f32`, SHA-256 `5a18da68540ab4e298982aec3e09e9ad412add28132d967d7632de3982233336`. That earlier raw artifact does not bind its producing binary, so this is not labeled a before/after precision-fix comparison. The current untraced and traced NumPy files both hash to `1e835b54a343f6ea4c51a3c18e74df5651a3dae827ff118af918fc85c6d2289c`.

All **81 required trace files**—attention and FFN output at each of 40 layers, plus terminal logits—have exact expected sizes. The terminal trace agrees with the returned diagnostic logits. In total, 384 native trace files occupy 279,763,072 bytes; their [per-file hashes](final-run/manifest.json) are retained, while the complete tensors remain in the recorded VM and local `/tmp` directories. This compact bundle includes the primary logits, reference last-row logits, logs, sources and settings under [final-run](final-run/).

The [bounded stage comparison](final-stage-comparison.json) checks 247 available native/reference stage pairs, using the [retained analysis script](analyze-stages.py). Only the embedding and first attention input meet the strict tolerance. The embedding is exact; first attention input differs by at most `2.98e-8` (relative L2 `5.43e-8`). The first material difference appears at the query after the first layer's two Q2_K projections:

| First-layer stage | Relative L2 | Maximum absolute error |
| --- | ---: | ---: |
| Attention input | 0.0000000543 | 0.0000000298 |
| Query | 0.010063 | 0.224803 |
| Quantized raw KV | 0.017346 | 0.25 |
| Attention output | 0.022265 | 0.086899 |
| FFN output | 0.018310 | 0.162665 |

[Projection metadata and source evidence](projection-type-evidence.json) confirm Q2_K query/KV weights. The reference method evaluates dequantized matrices with F32 inputs; ggml's quantized CUDA paths construct Q8_1 activations. This difference is consistent with the early divergence, but the individual kernels were not profiled and the entire final-logit discrepancy is **not fully attributed**. No query-weight reread, new GPU control run, tolerance change or inference-code change was made during this analysis. Exact stage hashes, hidden-tensor name aliases and all remaining differences are preserved in the report.
