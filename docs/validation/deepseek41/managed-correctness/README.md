# Final managed correctness lane

The latest full non-GPU lane passed **3,651/3,651 tests locally and on the
requested VM**, with zero failures or skips. The
[final remote manifest](remote-responses-audio-correctness.json) retains the
commands and hashes. Smaller counts below label preserved earlier source
stages; they are not alternative totals for the final build.

This lane covers managed architecture/loading gates, chat/tokenizer/DSML handling, image/video request preparation, exact Pillow preprocessing fixtures, tools-plus-JSON final turns, delayed thinking grammar, explicit unsupported-audio rejection, automatic GPU-count overrides, per-request thinking-budget closure, independent grammar forks, concurrent native-slot accounting, and existing non-GPU regressions. The earlier thinking-budget and native-slot accounting stage passed **3,433/3,433 tests** locally and on the requested Linux VM.

The tool-grammar, ordered byte-trigger, reasoning-loop, nested-map and integer-constant stage passed **3,541/3,541 tests** locally and on the VM, with zero failures or skips. `local-tool-grammar-release-correctness.*` and `remote-tool-grammar-3541-correctness.*` preserve that stage. Checks include HTTP policy errors before SSE headers, unchanged behavior for other architectures, lossless reserved-delimiter string arguments, arbitrary nested maps, exact Int64 constant boundaries, and fresh grammar state for internal skill rounds. `local-tool-grammar-correctness.*` retains the preceding successful 3,526-test local stage.

The subsequent history fix passed **3,544/3,544 tests locally and on the VM**, including a 213-test local focused run. `local-tool-history-correctness.*` and `remote-tool-history-3544-correctness.*` record that verification; the local manifest also retains the two failing before-fix cases. The fix preserves delimiter-bearing strings and nested JSON through generation, parsing and later tool-history rendering.

The earlier combined Unicode stage passed **3,566/3,566 tests locally and on the VM**, with zero failures or skips. It permits valid byte-fallback tokens that end within a Unicode scalar and rejects malformed UTF-8 in active grammar masks. `local-tool-utf8-correctness.*` and `remote-tool-utf8-correctness.*` record the commands, counters and hashes; the VM manifest also verifies that the previous private host stayed unchanged. The first local full attempt had one unchanged retained-cache test fail with 16 pooled reused tokens instead of zero. The whole 28-test class and the full lane passed on unchanged reruns; the initial failure is retained in `local-tool-utf8-first-attempt.log`, with the focused rerun and a qualified timing-race explanation in the manifest.

The partial-class follow-up makes partial Unicode tokens viable only when at least one valid scalar can satisfy a character class, including the union of negated ranges. It passed **3,582/3,582 tests locally and on the VM**, with a 221-test local focused run (`local-partial-class-correctness.*` and `remote-partial-class-correctness.*`). Its isolated VM host is `server-v41-final3582`; the then-active 3,566-stage host used for matched placement measurements remained unchanged. Its new cases concern generic Unicode character classes outside the V4.1 JSON/tool schemas.

An independent [mask-equivalence check](../grammar-mask-equivalence/README.md) used the retained 3,566-stage and final 3,582-stage local assemblies with the actual Q2 vocabulary. All **785 visited masks across 14 JSON/tool cases were byte-identical**. This establishes equivalence at those fixture states, not exhaustive grammar or full-model inference equivalence. Earlier actual-tokenizer reports remain intact.

A subsequent raw tag-family serialization experiment passed **3,597/3,597 tests locally and on the VM**, with 215 local focused checks (`local-tool-tag-family-correctness.*` and `remote-tool-tag-family-correctness.*`). The isolated VM host is `server-v41-final3597`; its native b26 binary and the preserved 3,566/3,582 host files remained unchanged throughout staging and testing. It reserves `<param`, `</param`, `<invoke` and `</invoke` within raw strings after full-checkpoint failures used mistyped tags as unfinished argument data. Ordinary XML/comparisons remain valid, and reserved-family literal values round-trip through escaped JSON strings and tool history. Parser behavior is unchanged. The independent [actual-tokenizer replay](../tool-serialization-replay/README.md) shows the old response stayed inside an unfinished string and the new grammar blocks the first mistyped family token while preserving the tested valid alternatives. Later full-checkpoint results below use a combined managed/native profile, not an isolated grammar A/B; earlier failed responses are preserved.

The image request-validation follow-up passed **3,635/3,635 tests locally and on the VM**, with **91 local focused checks**. `local-image-url-correctness.*` and `remote-image-url-correctness.*` record both OpenAI chat `image_url` and Responses `input_image` rejection before streaming, planning or uploads. Remote/malformed URLs and invalid/empty base64 return JSON 400, while valid data URIs, multiple/history images, and other architectures retain their existing behavior. Each host's Runtime DLL is byte-identical to its 3,597 stage. The isolated host is `server-v41-final3635`, using the verified native `3b885898…` archive; active and earlier private host files remained unchanged. The first local full attempt's retained-symbol test incorrectly included a test-only CPU-pool observer; `local-image-url-first-attempt.*` retains that failure. Using the guarded `TSG_TEST_EXPORT` alias corrected the source scan without adding an undefined production symbol to the iOS manifest. The whole affected test class and full lane then passed.

The first VM attempt passed **3,634/3,635**, with the same intermittent retained-cache assertion recorded at the earlier Unicode stage; `remote-image-url-first-attempt.*` preserves it. Its [bounded local diagnosis](../retained-cache-admission/README.md) reproduced legitimate pooled-prefix reuse when the first request ran before its partner was admitted. The original holder was correctly discarded; this was a test-path assumption. The one test now synchronizes paired admission through the existing compute gate, preserving its assertions and all Runtime code. All 120 gated diagnostic trials passed. The whole 28-test cache class and the full 3,635-test lane then passed locally and on the VM, with no skips or failures.

The Responses audio preflight follow-up passed **3,651/3,651 tests locally and on the VM**, with **84 focused audio/image/API checks on both hosts** and zero failures or skips. `local-responses-audio-correctness.json` preserves the before-fix result (13 rejection failures and three passing compatibility controls), the corrected runs and source/binary hashes. `remote-responses-audio-correctness.{json,log}` records the isolated `server-v41-final3651` build using archived native `6b3b5ab3…`; every existing private host and the ready host remained unchanged through testing. V4.1 `input_audio` and `audio_url` parts now fail with JSON 400 before payload decoding, uploads, planning or SSE, including malformed or missing audio payloads and audio in later messages. Other architectures and the default parser caller retain their behavior. The Runtime DLL is unchanged from the 3,597/3,635 stages.

The subsequently loaded full-checkpoint routed-TP host used this 3,651-stage managed build with native `6b3b5ab3…`. Its real HTTP [image suite passed 8/8](../full-checkpoint/tp8-context65536-ubatch1024-cpumoe0-cputhreads32-sparse1-compact1-chunk1024-6b3-final-image-input-rejections.json) and [Responses audio suite passed 4/4](../full-checkpoint/tp8-context65536-ubatch1024-cpumoe0-cputhreads32-sparse1-compact1-chunk1024-6b3-final-audio-input-rejections.json), with JSON 400 before SSE in streaming and non-streaming modes. Reports retain exact request/response bodies and hashes. These twelve input-validation checks are separate from inference quality.

That final TP profile passed [30/30 tool-policy cases](../full-checkpoint/tp8-context65536-ubatch1024-cpumoe0-cputhreads32-sparse1-compact1-chunk1024-6b3-final-tool-policies.json), including the previously failing named-thinking case, and [4/4 thinking workflows](../full-checkpoint/tp8-context65536-ubatch1024-cpumoe0-cputhreads32-sparse1-compact1-chunk1024-6b3-final-thinking.json), including the earlier agent markup failure. Non-streaming workflows passed 4/4, media 25/25, Chinese/Unicode JSON 10/10, separately serial tools 10/10, sustained decode 15/15, and long retrieval 2/2. Overall, **129/130 inference cases passed**. The default-parallel quality group passed 29/30 versus the initial TP profile's 30/30: one request still called `calculate_total` with zero placeholders alongside `read_invoice` before obtaining the invoice data. That failure remains rejected and preserved. The [final placement records](../final-placements/README.md) distinguish this combined native/thread/managed profile from an isolated grammar comparison; these results do not establish a blanket quality gain or llama.cpp parity.

The final layer-split host used the same verified 3,651-stage managed build and native `6b3b5ab3…` and passed **138/138 inference cases**. Its [default-parallel quality group passed 30/30](../full-checkpoint/layer8-context65536-ubatch1024-cpumoe0-cputhreads32-sparse1-compact1-chunk1024-6b3-final-quality.json), with 30/30 tool policies, 4/4 thinking, 4/4 non-streaming, 25/25 media, 10/10 Chinese/Unicode, 10/10 separate serial workflows, 15/15 sustained decode, 2/2 single-request long retrieval, and [8/8 concurrent long requests](../full-checkpoint/layer8-context65536-ubatch1024-cpumoe0-cputhreads32-sparse1-compact1-chunk1024-6b3-final-long-parallel.json). The [concurrent-long native accounting](../full-checkpoint/layer8-context65536-ubatch1024-cpumoe0-cputhreads32-sparse1-compact1-chunk1024-6b3-final-long-parallel-runs.json) records 153,187 prefill tokens including warmup, with zero preemptions. These are full-checkpoint measurements, separate from the 3,651-test managed lane.

| Final profile | Default-parallel quality | Its complete inference plan |
|---|---:|---:|
| Eight-GPU layer split | 30/30 | 138/138 |
| Eight-GPU routed TP | 29/30 | 129/130 |
| Four routed layers offloaded to CPU | 28/30 | 55/57 |

The plans differ: layer split adds eight concurrent long requests to the TP suite, while CPU offload uses the smaller quality/decode/long/serial suite. Layer success does not erase the one TP or two CPU-offload dependent-tool failures. The [local diagnosis](../parallel-tool-dependency/README.md) preserves the model-emitted calls and found no prompt/grammar/parser defect forcing the extra call or zero values. No numerical-path isolated A/B or llama.cpp parity is inferred.

The independent [actual-tokenizer grammar probe](../actual-tokenizer-grammar-maps.json) passed **12/12 cases** using the supplied Q2_K checkpoint's full 129,280-token vocabulary. Every encoded output token was checked against its grammar mask before incremental UTF-8 decoding and strict parser validation. The probe covers ordered thinking activation, Unicode/XML strings, escaped delimiter-bearing JSON values, arbitrary nested maps, and malformed/unknown call rejection. Its predecessor [nine-case report](../actual-tokenizer-grammar.json) remains unchanged.

The separate [generic JSON Unicode probe](../json-unicode/README.md) passed **16/16 actual-tokenizer cases** after demonstrating the old runtime's literal/split emoji rejection and malformed UTF-8 acceptance. Its 22 new regression tests include bytewise decoding of every valid Unicode scalar. Neither tokenizer-only probe establishes full-model answer quality.

The filter was:

```text
Category!=Bench&Requires!=Models&Requires!=Cuda&Requires!=Mlx&Requires!=Video
```

This excludes opt-in model, GPU, real video decoder and benchmark tests; full-checkpoint and native GPU evidence is recorded separately in the main validation report. Video request tests in this lane use deterministic decoded frames.

`managed-budget-correctness.{json,log}` retain the 3,433-stage remote commands, counters, binary/TRX hashes and evidence that existing private server directories remained unchanged. Its isolated host is `/workspace/deepseek41-work/server-v41-budget`; the VM TRX is `/workspace/deepseek41-work/test-results/managed-budget-correctness.trx`. `local-budget-scheduler-correctness.{json,log}` retain the corresponding local commands, counters and source/binary/TRX hashes. Later remote tool stages use the separate `server-v41-tools` directory and retain their own manifests.

Earlier `managed-release-*` and `local-release-*` artifacts retain the successful 3,400-test stage; `managed-thinking-*` retains the preceding 3,388-test stage. The first thinking-budget implementation separately passed 3,416 tests before the request-local grammar-fork and scheduler-capacity regressions brought the combined count to 3,433. Earlier stages are retained as provenance, not substituted for newer verification.

The earlier tool-grammar stage's Python inference validation harness separately passed **29 tests on the VM** (`tools-validation-harness.log`), including the tool-policy scenarios available then. The previous 17-test local/VM stage remains in `budget-validation-harness.log`. These retained stages exercise reasoning-only SSE output, incorrect content with correct reasoning, a reasoning-only multi-turn acknowledgement, and rejection of incomplete comparison reports. Reports retain token/timing metrics and separate reasoning, but only `assistant_message.content` becomes `validated_content` for answer correctness.
